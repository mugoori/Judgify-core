# TriFlow AI v0.3.0 NSIS 마이그레이션 가이드

**작성일**: 2025-11-17
**대상**: TriFlow AI v0.2.x 기존 사용자

---

## 📌 변경 사항 요약

### MSI → NSIS 전환 이유

**문제**: MSI 인스톨러에서 자동 업데이트 사용시 구버전이 그대로 남아 중복 설치
- 사용자가 Settings에서 "업데이트 확인" 버튼 클릭
- 신규 버전이 별도로 설치됨
- Windows "설치된 앱"에 TriFlow AI가 2개로 표시
- 구버전을 수동으로 제거해야 하는 불편함

**해결**: NSIS 인스톨러로 전환하여 자동 업데이트 정상 작동
- 자동 업데이트시 구버전 자동 제거
- 항상 1개의 TriFlow AI만 유지
- 원클릭 업데이트 경험 제공

### 사용자 영향

| 사용자 그룹 | 영향 | 조치 필요 |
|------------|------|----------|
| **기존 v0.2.x 사용자** | 수동 재설치 필요 (1회만) | ✅ 필수 |
| **v0.3.0 신규 설치** | 영향 없음 | ❌ 불필요 |
| **v0.3.0 이후 업데이트** | 자동 업데이트 정상 작동 | ❌ 불필요 |

---

## 🚀 마이그레이션 방법

### Step 1: 기존 버전 제거

1. **Windows 설정** 열기
   - `Windows + I` 키 또는
   - 시작 메뉴 → 설정 아이콘 클릭

2. **앱** → **설치된 앱** 선택

3. **"TriFlow AI" 검색**
   - 모든 버전이 표시됨 (v0.2.x 등)

4. **각 버전별로 제거**
   - 버전 옆 `⋮` (점 3개) 클릭
   - "제거" 선택
   - 확인 대화상자에서 "제거" 클릭

**주의**:
- ✅ 데이터는 안전하게 보관됩니다 (아래 FAQ 참조)
- ⚠️ 모든 버전을 제거해야 합니다

---

### Step 2: v0.3.0 NSIS 설치

1. **GitHub Release 페이지 방문**
   ```
   https://github.com/mugoori/Judgify-core/releases/tag/v0.3.0
   ```

2. **NSIS 인스톨러 다운로드**
   - 파일명: `TriFlow-AI_0.3.0_x64-setup.exe`
   - 크기: 약 3.0MB

   **⚠️ 주의**: `.msi` 파일은 사용하지 마세요!
   - MSI 파일도 제공되지만 자동 업데이트 문제 존재
   - NSIS (`.exe`) 파일만 사용하세요

3. **다운로드한 파일 실행**
   - `TriFlow-AI_0.3.0_x64-setup.exe` 더블클릭
   - Windows Defender SmartScreen 경고 발생시:
     - "추가 정보" 클릭
     - "실행" 클릭

4. **설치 마법사 따라가기**
   - 설치 경로 선택 (기본값 권장)
   - 바탕화면 바로가기 생성 (선택사항)
   - "설치" 버튼 클릭

5. **설치 완료**
   - "완료" 버튼 클릭
   - TriFlow AI 자동 실행

---

### Step 3: 데이터 확인 및 자동 업데이트 테스트

1. **TriFlow AI 실행**
   - 바탕화면 바로가기 또는
   - 시작 메뉴에서 "TriFlow AI" 검색

2. **기존 데이터 확인**
   - Chat Interface 탭: 이전 채팅 히스토리 확인
   - Workflow Builder 탭: 저장된 워크플로우 확인
   - Settings 탭: API 키 및 설정 확인

3. **자동 업데이트 테스트** (선택사항)
   - Settings 탭 → "업데이트 확인" 버튼 클릭
   - "현재 최신 버전입니다" 메시지 확인

4. **향후 자동 업데이트**
   - v0.3.1, v0.4.0 등 신규 버전 출시시
   - Settings에서 "업데이트 확인" 클릭
   - 자동 다운로드 및 설치 진행
   - 구버전 자동 제거 ✅

---

## ❓ FAQ (자주 묻는 질문)

### Q1: 기존 데이터는 유지되나요?

**A**: 네, 모든 데이터는 안전하게 보관됩니다!

**데이터 저장 위치**:
```
%APPDATA%\com.triflow.desktop\

예시:
C:\Users\YourName\AppData\Roaming\com.triflow.desktop\
```

**보관되는 데이터**:
- ✅ 채팅 히스토리 (Chat Interface)
- ✅ 워크플로우 정의 (Workflow Builder)
- ✅ 피드백 데이터 (자동학습용)
- ✅ API 키 및 설정 (Settings)

**주의**:
- Windows 앱 제거는 **프로그램 파일만** 삭제
- 사용자 데이터는 `%APPDATA%`에 남아있음
- 수동 재설치해도 모든 데이터 그대로 유지

---

### Q2: MSI 파일도 함께 제공되는데 어떤 걸 사용하나요?

**A**: **NSIS (`.exe`) 사용을 강력히 권장**합니다!

| 인스톨러 | 파일명 | 자동 업데이트 | 권장도 |
|---------|--------|--------------|--------|
| **NSIS** | `TriFlow-AI_0.3.0_x64-setup.exe` | ✅ 정상 작동 | ⭐⭐⭐⭐⭐ |
| MSI | `TriFlow-AI_0.3.0_x64_en-US.msi` | ❌ 중복 설치 문제 | ⚠️ 비권장 |

**MSI 파일을 제공하는 이유**:
- 기업 환경에서 Group Policy로 배포시 필요
- 일반 사용자는 NSIS 사용 권장

---

### Q3: v0.2.x에서 자동 업데이트가 안 되는 이유는?

**A**: MSI → NSIS 전환은 **Breaking Change**입니다.

**기술적 이유**:
- v0.2.x 앱은 `latest.json`에서 **MSI 업데이트**를 찾음
- v0.3.0부터 `latest.json`이 **NSIS 업데이트**만 제공
- 인스톨러 형식이 다르므로 자동 업데이트 불가능

**해결 방법**:
- 1회 수동 재설치 필요
- v0.3.0 설치 후부터는 자동 업데이트 정상 작동

---

### Q4: 향후 자동 업데이트는 작동하나요?

**A**: 네, **v0.3.0 이후부터는 완전히 정상 작동**합니다!

| 업데이트 경로 | 작동 여부 | 방법 |
|--------------|---------|------|
| v0.2.4 → v0.3.0 | ❌ 수동 재설치 | 이 가이드 따라하기 |
| v0.3.0 → v0.3.1 | ✅ 자동 업데이트 | Settings에서 "업데이트 확인" |
| v0.3.1 → v0.4.0 | ✅ 자동 업데이트 | Settings에서 "업데이트 확인" |
| v0.4.0 → 향후 버전 | ✅ 자동 업데이트 | Settings에서 "업데이트 확인" |

**자동 업데이트 흐름**:
1. Settings → "업데이트 확인" 버튼 클릭
2. 새 버전 감지 → "업데이트 다운로드" 알림
3. 다운로드 완료 → "업데이트 설치" 버튼 표시
4. 버튼 클릭 → TriFlow AI 자동 재시작
5. 구버전 자동 제거 + 신규 버전 설치 ✅

---

### Q5: 데이터를 백업하려면 어떻게 하나요?

**A**: 수동 백업은 다음과 같이 진행하세요 (선택사항):

**Windows 탐색기에서**:
1. `Windows + R` 키 → `%APPDATA%\com.triflow.desktop` 입력 → Enter
2. 전체 폴더 복사
3. 안전한 위치에 붙여넣기 (예: `D:\Backups\TriFlow-AI\`)

**파일 구성**:
```
com.triflow.desktop/
├── triflow.db           # SQLite 데이터베이스 (모든 데이터)
├── triflow.db-shm       # 임시 파일
├── triflow.db-wal       # Write-Ahead Log
└── logs/                # 앱 로그 (선택사항)
```

**복원 방법**:
- 백업한 폴더를 동일 위치에 덮어쓰기
- TriFlow AI 재시작

**주의**:
- 일반적으로 백업 불필요 (재설치해도 데이터 유지)
- 완전히 안전한 백업을 원하는 경우에만 수행

---

### Q6: 설치 중 오류가 발생하면 어떻게 하나요?

**A**: 다음 순서로 해결하세요:

**1단계: Windows Defender SmartScreen 경고**
```
"Windows에서 PC 보호" 경고 발생시:
→ "추가 정보" 클릭
→ "실행" 버튼 클릭
```

**2단계: 구버전 완전 제거 확인**
```
Windows 설정 → 앱 → 설치된 앱
→ "TriFlow" 검색
→ 모든 버전 제거 확인
```

**3단계: 관리자 권한으로 설치**
```
TriFlow-AI_0.3.0_x64-setup.exe 우클릭
→ "관리자 권한으로 실행" 선택
```

**4단계: 여전히 문제 발생시**
- GitHub Issues에 문의: https://github.com/mugoori/Judgify-core/issues
- 오류 메시지 스크린샷 첨부
- Windows 버전 명시 (예: Windows 11 23H2)

---

### Q7: v0.3.0에서 v0.2.x로 다운그레이드 가능한가요?

**A**: 기술적으로 가능하지만 **권장하지 않습니다**.

**다운그레이드 방법** (비권장):
1. v0.3.0 제거
2. v0.2.x MSI 파일 다운로드 (GitHub Releases)
3. 설치 진행

**문제점**:
- ⚠️ v0.3.0에서 추가된 기능 사용 불가
- ⚠️ v0.3.0 이후 버전으로 자동 업데이트 불가
- ⚠️ 보안 패치 및 버그 수정 적용 안 됨

**권장 사항**:
- v0.3.0 유지하고 버그 발견시 GitHub Issues에 보고
- 팀이 신속히 수정 (핫픽스 릴리스)

---

## 🔧 기술 세부사항 (개발자용)

### Tauri v1 버그 우회

**GitHub Issue**: [tauri-apps/tauri#7349](https://github.com/tauri-apps/tauri/issues/7349)

**증상**:
- `targets: "nsis"`로 설정시 `.nsis.zip` 파일 생성 안 됨
- tauri-action@v0의 `includeUpdaterJson: true` 옵션 무효

**해결 방법**:
```json
// src-tauri/tauri.conf.json
{
  "bundle": {
    "targets": "all"  // "nsis" 대신 "all" 사용
  }
}
```

**트레이드오프**:
- ✅ NSIS ZIP 파일 정상 생성
- ⚠️ MSI 파일도 함께 생성 (릴리스 크기 증가)

**향후 계획**:
- Tauri v2로 업그레이드시 버그 해결
- `targets: "nsis"` 단독 설정 가능

---

### 릴리스 파일 구성

**v0.3.0 GitHub Release 파일 목록**:

**NSIS (주요, 사용자용)**:
```
TriFlow-AI_0.3.0_x64-setup.exe          # 설치 파일 (3.0MB)
TriFlow-AI_0.3.0_x64-setup.nsis.zip     # 자동 업데이트 번들
TriFlow-AI_0.3.0_x64-setup.nsis.zip.sig # 서명 파일 (보안)
```

**MSI (부가, 기업용)**:
```
TriFlow-AI_0.3.0_x64_en-US.msi          # MSI 설치 파일
TriFlow-AI_0.3.0_x64_en-US.msi.zip      # MSI 업데이트 번들
TriFlow-AI_0.3.0_x64_en-US.msi.zip.sig  # MSI 서명 파일
```

**자동 업데이트 매니페스트**:
```json
latest.json  # GitHub Pages 호스팅 (NSIS URL 참조)
```

**서명 검증**:
- Tauri 내장 서명 검증 시스템
- 공개키: `tauri.conf.json`의 `updater.pubkey`
- 개인키: GitHub Secrets에 안전하게 저장
- 자동 업데이트시 서명 자동 검증

---

### 자동 업데이트 매니페스트 (latest.json)

**호스팅 위치**:
```
https://mugoori.github.io/Judgify-core/latest.json
```

**파일 구조**:
```json
{
  "version": "0.3.0",
  "notes": "TriFlow AI Desktop Application 업데이트",
  "pub_date": "2025-11-17T08:04:43Z",
  "platforms": {
    "windows-x86_64": {
      "signature": "dW50cnVzdGVkIGNvbW1lbnQ...",
      "url": "https://github.com/mugoori/Judgify-core/releases/download/v0.3.0/TriFlow-AI_0.3.0_x64-setup.nsis.zip"
    }
  }
}
```

**업데이트 흐름**:
1. TriFlow AI 앱 시작시 `latest.json` 다운로드
2. 버전 비교 (현재 0.3.0 vs latest.json)
3. 새 버전 발견시 알림 표시
4. 사용자 확인 후 `.nsis.zip` 다운로드
5. 서명 검증 (`.nsis.zip.sig` 파일 사용)
6. ✅ 검증 성공 → 설치 진행
7. ❌ 검증 실패 → "서명 불일치" 오류

---

### GitHub Actions 워크플로우

**주요 개선사항** (v0.3.0):

1. **includeUpdaterJson 옵션 추가**:
   ```yaml
   # .github/workflows/release.yml
   - name: Build Tauri App
     uses: tauri-apps/tauri-action@v0
     with:
       includeUpdaterJson: true  # ← 추가
   ```

2. **Asset 감지 패턴 수정**:
   ```javascript
   // .nsis.zip 및 .nsis.zip.sig 정확히 탐지
   const nsisAsset = assets.find(a => a.name.endsWith('.nsis.zip'));
   const sigAsset = assets.find(a => a.name.endsWith('.nsis.zip.sig'));
   ```

3. **에러 메시지 개선**:
   ```javascript
   // Available assets 목록 표시
   core.setFailed(
     `NSIS updater files not found!\n` +
     `Available assets:\n${assets.map(a => `  - ${a.name}`).join('\n')}`
   );
   ```

---

## 📚 추가 참고 자료

### 공식 문서
- **Tauri Updater Guide**: https://v2.tauri.app/plugin/updater/
- **Tauri Issue #7349**: https://github.com/tauri-apps/tauri/issues/7349
- **NSIS 공식 사이트**: https://nsis.sourceforge.io/

### 프로젝트 문서
- **TASKS.md Phase 10**: [TASKS.md#phase-10](../../TASKS.md#-phase-10-v030-nsis-마이그레이션-2025-11-17)
- **Tauri 서명 키 관리**: [CLAUDE.md#tauri-서명-키-관리-전략](../../CLAUDE.md#-tauri-서명-키-관리-전략)
- **전체 변경 이력**: [CHANGELOG.md](../../CHANGELOG.md)

### 지원 채널
- **GitHub Issues**: https://github.com/mugoori/Judgify-core/issues
- **Release 페이지**: https://github.com/mugoori/Judgify-core/releases

---

## 🙏 감사합니다

자동 업데이트 문제를 해결하기 위해 NSIS로 전환했습니다.

**1회 재설치의 불편함**이 있지만, 향후 **원클릭 자동 업데이트**를 위한 필수 조치입니다.

궁금한 점은 [GitHub Issues](https://github.com/mugoori/Judgify-core/issues)에 남겨주세요!

---

**문서 버전**: 1.0
**최종 업데이트**: 2025-11-17
**다음 업데이트 예정**: v0.3.1 릴리스 후
