# prp-example.md

이 문서는 Claude 판단 엔진이 PRP(Planning - Reasoning - Prompting) 구조에 따라 일관되고 해석 가능한 판단을 수행할 수 있도록, 실제 적용 가능한 예시를 중심으로 구성되어 있습니다.

---

## 0. 문서 목적
- PRP는 단순한 코드 생성 단계를 넘어서, 판단의 근거와 흐름을 명시적으로 구조화하여 LLM 기반 시스템이 추론 가능한 행동을 하도록 설계합니다.
- 이 문서의 예시는 Supabase + MCP 기반 MVP 시스템에서 자주 사용되는 판단 구조를 반영합니다.

---

## 1. 기본 구조 요약
PRP는 다음 3단계로 구성됩니다:

1. **Planning**: 판단 목적, 조건, 규칙을 명확히 정의
2. **Reasoning**: 실제 조건 평가 및 로직 실행
3. **Prompting**: 사용자 응답 또는 후속 판단을 위한 메시지 생성

---

## 2. PRP 예시 - 작업자 호출 판단

### 2.1 Planning 단계
```yaml
purpose: 온도/습도 기준에 따른 작업자 호출 판단
inputs:
  - temp: 실시간 측정 온도 (℃)
  - humidity: 실시간 습도 (%)
conditions:
  - temp > 85
  - humidity < 20
logic:
  - 조건을 모두 만족하면 CALL_OPERATOR
  - 아니면 NO_ACTION
output:
  - result: CALL_OPERATOR 또는 NO_ACTION
```

### 2.2 Reasoning 단계
```python
def evaluate_conditions(temp, humidity):
    if temp > 85 and humidity < 20:
        return "CALL_OPERATOR"
    else:
        return "NO_ACTION"
```

### 2.3 Prompting 단계
- 사용자 응답 메시지 (자연어 요약):
```text
현재 온도 88℃, 습도 18%로 기준을 초과하였기 때문에 작업자 호출이 필요합니다.
```
- 판단 로그용 메시지:
```json
{
  "input": {"temp": 88, "humidity": 18},
  "result": "CALL_OPERATOR",
  "explanation": "온도 > 85 AND 습도 < 20 조건을 모두 만족함.",
  "engine": "DSL",
  "timestamp": "2025-08-05T15:00:00"
}
```

---

## 3. PRP 예시 - 문서 자동 분류

### 3.1 Planning
```yaml
purpose: 문서 유형 분류 (계약서, 보고서, 회의록 등)
inputs:
  - title: 문서 제목
  - body: 문서 본문
logic:
  - 키워드 매칭 및 텍스트 패턴 분석
  - 예: "계약", "체결", "기간" 포함 시 계약서로 분류
output:
  - doc_type: 계약서 / 보고서 / 회의록 중 하나
```

### 3.2 Reasoning
```python
if "계약" in body and "체결" in body:
    doc_type = "계약서"
elif "회의" in title or "회의록" in body:
    doc_type = "회의록"
else:
    doc_type = "보고서"
```

### 3.3 Prompting
```text
이 문서는 '계약', '체결'이라는 표현이 포함되어 있어 계약서로 분류됩니다.
```

---

## 4. PRP 예시 - Git 커밋 자동 생성

### 4.1 Planning
```yaml
purpose: 변경사항 요약 후 커밋 메시지 자동 생성
inputs:
  - diff_summary: 코드 변경 요약 (라인 단위)
logic:
  - 주요 함수명, 수정된 기능, 추가/삭제 여부 판단
output:
  - commit_message: 요약된 커밋 메시지
```

### 4.2 Reasoning
```python
if "add" in diff_summary and "login" in diff_summary:
    commit_message = "✨ 사용자 로그인 기능 추가"
elif "fix" in diff_summary:
    commit_message = "🐛 로그인 오류 수정"
```

### 4.3 Prompting
```text
로그인 기능이 새롭게 추가되었으므로 '✨ 사용자 로그인 기능 추가'라는 커밋 메시지를 생성했습니다.
```

---

## 5. 기타 참고 사항
- 모든 PRP는 `initial.md` 기준에 따라 설계되며, 판단 실패 시에도 설명과 로그를 남겨야 함
- DSL이 아닌 자연어 조건일 경우, Claude는 자체 변환 후 조건 평가 진행
- MCP CLI 또는 Slack 메시지로 Prompting 결과 전송 가능

---

